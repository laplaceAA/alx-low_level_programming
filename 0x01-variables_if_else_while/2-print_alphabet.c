#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char vam;
	for (vam = 'a';  vam <= 'z'; vam++)
	{
		putchar(vam);
	}

	putchar('\n');

	return (0);

}
