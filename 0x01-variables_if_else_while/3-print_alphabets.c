#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	for (char vam ='a'; vam <='z'; vam++)
	{
		putchar(vam);
	}
	for (char vim ='A'; vim <='Z'; vim++)
	{
		putchar(vim);
	}
	putchar('\n');
	return (0);

}
