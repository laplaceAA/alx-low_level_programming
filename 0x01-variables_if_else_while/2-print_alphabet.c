#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main (void)
{

	char vam = 'a';
	for(; vam <= 'z'; vam++)
	{
		putchar(vam);
	}

	putchar('\n');

	return (0);

}
